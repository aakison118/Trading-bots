# Trading bot script

The bot uses an expected moving average strategy to automatically set trades using the OANDA API. Seperate config file is used to set the type, number and frequency of trades on the platform.
